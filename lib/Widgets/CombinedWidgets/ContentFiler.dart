import 'package:flutter/material.dart';

class ContentFiler extends StatelessWidget {
  const ContentFiler({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}